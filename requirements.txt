# Streamlit Cloud için optimize edilmiş bağımlılıklar
# CPU-only PyTorch kullanarak bellek tüketimi düşürülür (~5x daha küçük)

--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch CPU-only — Streamlit Cloud RAM sınırı için kritik
torch>=2.2.0
torchvision>=0.17.0

# Core LLM / RAG
langchain>=0.3.0
langchain-community>=0.3.0
langchain-anthropic>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0

# Vector DB
chromadb>=0.5.0

# Embedding (Türkçe destekli, çok dilli)
sentence-transformers>=3.0.0

# UI
streamlit>=1.40.0

# Data processing
pypdf>=5.0.0

# Utilities
python-dotenv>=1.0.0
